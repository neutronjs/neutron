import { toast } from 'react-toastify';

export default {
  info: (message) => {
    toast.info(message);
  },

  success: (message) => {
    toast.success(message);
  },

  error: (message) => {
    toast.error(message);
  },

  warning: (message) => {
    toast.warn(message);
  },
};
