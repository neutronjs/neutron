import React from 'react';
import { StyledContainer, StyledTitle } from './styles';

export default function <%= props.name %>() {
  return (
    <StyledContainer>
      <StyledTitle><%= props.name %> component!</StyledTitle>
    </StyledContainer>
  );
}
