import React from 'react';
import { Text, View } from 'react-native';

import styles from './styles';

export default function <%= props.name %>() {
  return (
    <View style={styles.container}>
      <Text style={styles.title}>Lala component!</Text>
    </View>
  );
}
